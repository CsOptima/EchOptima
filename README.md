🚀 EchOptima
EchOptima — это мощная платформа для интеллектуального анализа данных, построенная на микросервисной архитектуре. Система объединяет веб-интерфейс и Telegram-бота, предоставляя доступ к "умному" пайплайну из нескольких нейронных сетей для обработки текста и изображений.

🔗 Web-версия: echoptima.ru
🤖 Telegram Bot: @echoptimabot
Figma: https://www.figma.com/design/09qZTF4nr9PAmJtLitelJK/Echoptima

🏗 Архитектура
Проект реализован в виде распределенной системы микросервисов, развернутой в Kubernetes. Основной репозиторий организован через Git Submodules, объединяя следующие компоненты:

🧩 Сервисы
Сервис	Описание
EchOptimaBackend	Основной API шлюз и бизнес-логика. Оркестрирует запросы между ML-сервисами и базами данных.
EchOptimaFrontend-web	Пользовательский веб-интерфейс (SPA).
EchOptimaFrontend-tgbot	Интерфейс взаимодействия через Telegram.
EchOptimaNER-service	Сервис извлечения именованных сущностей (Named Entity Recognition).
EchOptimaSentiment-service	Сервис анализа тональности текста (Sentiment Analysis).
EchOptimaSummarizer-service	Сервис саммаризации (автоматического реферирования) текста.
EchOptimaImage-service	Сервис для обработки и анализа изображений.
🧠 Smart Pipeline
Данные проходят через цепочку нейросетей и обогащаются данными из внешних API, обеспечивая глубокий анализ входящей информации.

🛠 Технологический стек
Infrastructure: Kubernetes (K8s), Docker, Nginx.
Orchestration: Git Submodules for repo management.
Backend: Python (FastAPI).
ML/AI: PyTorch, Transformers.
Frontend: JavaScript Framework Vue, Telegram API.
🚀 Начало работы
Так как репозиторий использует субмодули, процесс клонирования отличается от стандартного.

1. Клонирование репозитория
```Bash

# Клонирование с инициализацией всех подмодулей
git clone --recursive https://github.com/your-username/EchOptima.git

# Если вы уже склонировали без флага recursive:
git submodule update --init --recursive
```

2. Деплой в Kubernetes
Конфигурации для деплоя (Manifests/Helm charts) находятся в директории k8s.

```Bash

# Пример применения конфигураций
kubectl apply -f k8s/namespace.yaml
kubectl apply -f k8s/secrets.yaml
kubectl apply -f k8s/services/
kubectl apply -f k8s/deployments/
```
Образы контейнеров подтягиваются из Docker Hub phlyash.

🔄 Работа с субмодулями
Если вы обновили код в одном из сервисов, не забудьте обновить ссылку в основном репозитории:
```Bash

# Обновление всех субмодулей до последних коммитов в их main ветках
git submodule update --remote

# Фиксация изменений версий субмодулей в основном репозитории
git add .
git commit -m "Update submodules to latest versions"
```
👨‍💻 Команда
arda-sketch: ml, manager
AlekseyKiryanov: backend
Phlyash: devops, frontend